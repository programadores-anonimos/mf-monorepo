{
  "name": "monorepo",
  "private": true,
  "scripts": {
    "start": "yarn workspaces run start",
    "serve": "wsrun --parallel start"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "concurrently": "^5.1.0",
    "lerna": "^4.0.0",
    "wsrun": "^5.2.0"
  }
}
